{
  "name": "spot-agent",
  "private": true,
  "scripts": {
    "client": "cd client && npm run dev",
    "api": "cd api && venv\\Scripts\\uvicorn main:app --reload --port 8000",
    "dev": "concurrently \"npm run client\" \"npm run api\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
